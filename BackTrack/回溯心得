同一层之间去重：如果可以排序 就是当前的和上一个比较
             如果不能排序，就在每一层维护一个used数组
不同层要驱去重：维护一个全局used数组

针对N皇后问题，通过行、列、主副对角线四个维度可以精确锁定某个点能否放置
每次查找和回溯只需要维护三个长度为N的一维数组（行在递归中自动避开）

针对数独问题，如果同一层有多个需要回溯，不应该想着回溯完当前层然后再跳到下一层
而是把每个空额的看成之前题目的一层，找到下一个需要空格再递归进去
